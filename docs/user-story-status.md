# RCH Rust Canonical User Story Status

This is the single canonical user-story ledger for release-candidate feature
audit work. It tracks operator-facing features, expected behavior, current
evidence, test status, and errors found during manual or automated story runs.

Last updated: 2026-06-23

## Status Values

- `Inventory`: Story identified from code/docs, expected behavior drafted, test pass not started.
- `Covered`: Automated tests or prior release evidence cover the expected behavior.
- `Manual pass`: A human/browser/live-device pass verified the behavior.
- `Needs manual test`: Automated coverage exists or source behavior is known, but the user workflow still needs an end-to-end manual pass.
- `Error found`: A logistical, UX, runtime, or behavioral error is recorded and needs a fix.
- `Retest needed`: A fix landed; the story must be exercised again.
- `Blocked`: A dependency, device, credential, or external service prevents meaningful testing.

## Canonical Table

| ID | Feature / surface | User story | Expected behavior | Current status | Evidence source | Errors / next action |
| --- | --- | --- | --- | --- | --- | --- |
| RCH-US-001 | Python-compatible northbound HTTP routes | As an existing RCH API/UI client, I can call the Python 2.9.x northbound route set against the Rust server. | Rust exposes the route inventory in OpenAPI, preserves method availability, returns Python-shaped success/error envelopes, and does not regress required routes. | Covered | `docs/release-contract-matrix.json` contract `python-northbound-http-routes`; `docs/release-parity-report.md`; `crates/r3akt-rch-server/src/lib.rs`; `crates/r3akt-rch-server/tests/release_contract_matrix.rs` | Expand into route-family story rows during route-by-route manual testing. |
| RCH-US-002 | Rust additive HTTP/control routes | As an operator, I can use Rust-only control, stream, OpenAPI, and app-info routes without confusing them for Python parity requirements. | Rust additive routes remain exposed, protected where needed, documented in OpenAPI where applicable, and do not break Python-compatible surfaces. | Covered | `docs/release-contract-matrix.json` contract `rust-additive-http-routes`; `crates/r3akt-rch-server/src/lib.rs` | Manual UX test stream/control routes from browser/API client. |
| RCH-US-003 | Authentication and connection selection | As a remote operator, I can configure a hub URL/API key and only enter protected UI routes after a valid auth session. | Public routes remain accessible; protected routes redirect to Connect when remote auth is missing; validation failure gives a clear error. | Manual pass | `ui/src/router/index.ts`; `ui/src/pages/ConnectPage.vue`; `ui/src/stores/connection.ts`; `/api/v1/auth/validate`; 2026-06-22 remote auth and connection retest below | Browser retest passed against a remote-looking `127.0.0.1.nip.io:18080` target: protected route redirected to Connect with `redirect=/chat`, missing API key showed clear validation, API-key login returned to Chat, and local loopback settings were restored. |
| RCH-US-004 | Dashboard/runtime control | As an operator, I can see runtime health and request backend, announce, and propagation sync actions. | Dashboard renders status, queue metrics, Reticulum runtime state, and action feedback without stale or misleading state. | Manual pass | `ui/src/pages/DashboardPage.vue`; `/Status`; `/diagnostics/runtime`; control routes; 2026-06-22 dashboard runtime-control retest below | Fixed misleading `Port: -` status by reporting the bound API listener in `/Control/Status`. Fixed propagation sync feedback so the dashboard waits for the backend sync window and surfaces the server error detail instead of a generic failure. |
| RCH-US-005 | Help, examples, and app info | As a new operator, I can inspect help, examples, app metadata, and route inventory without setup friction. | `/Help`, `/Examples`, `/api/v1/app/info`, `/openapi.json`, and `/openapi.yaml` respond with useful metadata and no auth surprises. | Manual pass | `docs/release-readiness-audit.md`; `crates/r3akt-rch-server/src/lib.rs`; release readiness smoke; 2026-06-22 public metadata smoke below; 2026-06-22 OpenAPI Windows parser retest below | Fixed and retested the Windows parser issue: `/openapi.json` no longer exports case-insensitive duplicate path keys, and the lower-case diagnostics route is represented through `x-rch-aliases`. |
| RCH-US-006 | Runtime diagnostics | As a release engineer, I can diagnose persistence, workers, Reticulum, outbound queues, counters, receipts, and schema state. | `/diagnostics/runtime` reports configured services, process/worker state, queue depth, dispatch/receipt counts, Reticulum/ZMQ configuration, and SQLite schema details. | Manual pass | `docs/release-contract-matrix.json` contract `rust-additive-runtime-diagnostics`; `docs/release-readiness-audit.md`; server tests; 2026-06-22 public metadata smoke below | Read-only local endpoint smoke passed; deeper diagnostics accuracy should still be checked when exercising queue/Reticulum failures. |
| RCH-US-007 | Config and Reticulum config management | As an operator, I can read, validate, apply, and roll back hub and Reticulum configuration. | Config routes return text payloads, validate INI-shaped content, apply changes with backups, and roll back safely. | Manual pass | `docs/release-contract-matrix.json`; `ui/src/pages/ConfigurePage.vue`; `crates/r3akt-rch-server/src/lib.rs` config tests; 2026-06-22 config and Reticulum path retest below; 2026-06-22 controlled config apply/rollback retest below; 2026-06-23 Reticulum form and BOM config retest below | Fixed startup resolution of `[hub].reticulum_config_path` so local testing uses the real Reticulum config when `--reticulum-config-path` is omitted, including BOM-prefixed Windows config files. Hub and Reticulum API apply/rollback, Hub UI apply/rollback, and Reticulum form UI apply/rollback all passed against isolated temp files. |
| RCH-US-008 | Topics and subscribers | As an operator, I can create, list, patch, delete, associate, and subscribe topics/subscribers. | Topic IDs normalize consistently; subscribers persist; pagination and Python-compatible envelopes work; UI updates after mutations. | Needs manual test | `docs/release-contract-matrix.json`; `crates/r3akt-rch-core/src/lib.rs`; `ui/src/pages/TopicsPage.vue`; 2026-06-23 topics/subscribers partial CRUD retest below; 2026-06-23 topic asset association API retest below | Topic edit plus subscriber add/edit/list passed in the browser, backend delete cleanup passed through `/Subscriber` and `/Topic`, and live API attach/detach/clear-on-topic-delete behavior passed for file/image topic associations. UI delete confirmation and rendered Manage Assets modal proof still need a clean browser pass. |
| RCH-US-009 | Chat direct, fanout, broadcast, and attachments | As an operator, I can send direct, topic, broadcast, and attachment messages and inspect delivery state. | Outbound messages are persisted immediately, worker dispatches asynchronously, direct timeouts can fall back to propagation when policy allows, and list/detail metadata stays understandable. | Retest needed | `crates/r3akt-rch-server/src/lib.rs`; PR #200 live canary `14d2c744ab6d436eb17ff2ccd987e2be`; `ui/src/pages/ChatPage.vue`; 2026-06-22 broadcast timeout retest below; 2026-06-22 propagated broadcast callback retest below; 2026-06-22 rate-limit budget retest below; 2026-06-23 persisted broadcast fallback repair retest below; 2026-06-23 superseded broadcast failure event retest below; 2026-06-23 dashboard stale broadcast failure card retest below; 2026-06-23 dashboard event snapshot reconciliation retest below; 2026-06-23 post-budget fallback retry retest below | Fixed worker-side broadcast direct `send_timeout` fallback to queued propagation, fixed propagated broadcast failure callbacks, fixed persisted retry-eligible fallback rows so they are repaired back to queued propagation, kept retryable propagated fallback errors queued after the fallback retry ceiling, made active retry attempts clear stale failure metadata, made recovered stale failure events replay as superseded, and fixed the dashboard event feed to remove older failure cards when superseded/recovery events arrive or periodic `/Events` snapshots omit them. Continue live stress after the daemon rate-limit window clears. |
| RCH-US-010 | Outbound delivery queue and retry policy | As an operator, I can trust queued sends, retry budgets, receipt timeouts, partial fanout handling, and failure reasons. | Queue metadata accurately distinguishes queued, in-progress, accepted, propagated, delivered, failed, dropped, retry, timeout, and receipt-pending states. | Retest needed | `crates/r3akt-rch-server/src/lib.rs`; `docs/release-live-stress-report.md`; PR #200; 2026-06-22 broadcast timeout retest below; 2026-06-22 propagated broadcast callback retest below; 2026-06-22 rate-limit budget retest below; 2026-06-23 persisted broadcast fallback repair retest below; 2026-06-23 superseded broadcast failure event retest below; 2026-06-23 dashboard event snapshot reconciliation retest below; 2026-06-23 post-budget fallback retry retest below | Fixed propagated broadcast fallback retry budget, extended rate-limit delayed retries, made `/internal/delivery-failure` reuse the retry scheduler, preserved the extended rate-limit retry budget across later timeouts, prevented already-started repaired rows from being selected repeatedly, kept post-budget retryable propagated fallback errors out of terminal failed state, cleared stale failure metadata on retry start and success, made event replay reflect recovered current state, and made dashboard refresh reconcile against authoritative `/Events`. Retest with live phones/decks until propagated dispatch reaches accepted/sent state. |
| RCH-US-011 | Reticulumd ZeroMQ runtime | As a release engineer, I can run RCH against LXMF-rs reticulumd through ZeroMQ command/response endpoints. | Server dispatches normal RCH payloads, batched fanout, event polling, delivery status, and identity announce calls through the ZeroMQ SDK boundary. | Covered | `docs/release-contract-matrix.json` contract `rust-additive-reticulumd-runtime`; `crates/r3akt-transport-rns/src/lib.rs`; CI checks on PR #200 | Keep live-device coverage current after transport or dependency changes. |
| RCH-US-012 | Reticulum/LXMF legacy-compatible command surface | As a peer/client, I can send legacy/plugin command names and payloads that Rust understands like Python RCH. | Legacy command aliases, admin commands, telemetry request, attachments, and command reply shapes remain accepted where required. | Covered | `docs/release-contract-matrix.json` contract `python-lxmf-legacy-command-surface`; `crates/r3akt-rch-core/src/lib.rs`; `crates/r3akt-profile-rch/src/lib.rs` | Add manual inbound command story when live peer scripting is available. |
| RCH-US-013 | REM compatibility and southbound command fanout | As a REM phone/deck user, I receive supported mission, checklist, EAM, log, marker, and telemetry commands in REM-compatible LXMF fields. | RCH emits reduced `FIELD_COMMANDS` payloads, deduplicates peers, respects REM capability/mode, and reports delivery/partial failures clearly. | Manual pass | `docs/rem-southbound-interface.md`; `docs/release-live-stress-report.md`; `docs/release-readiness-audit.md` | Continue two-phone plus raphydeck/silkedeck/corvodeck stress; raphydeck presence still needs current verification. |
| RCH-US-014 | Client, identity, REM peer, and routing workspace | As an operator, I can inspect and manage clients, identities, REM peers, teams, routing, bans, blackholes, and joins. | UI lists clients/identities/REM peers; ban/blackhole/unban/remove/join actions call the correct API and reflect errors clearly. | Needs manual test | `ui/src/pages/UsersPage.vue`; `/Client`; `/Identities`; `/api/rem/peers`; `/Command/DumpRouting`; 2026-06-23 client/identity API moderation retest below | Safe backend action flow passed with a disposable identity. Browser action proof remains open because the in-app browser timed out at the protocol layer before page interaction. |
| RCH-US-015 | Team roster and rights matrix | As a mission admin, I can manage teams, members, client links, and operation rights. | Team/member CRUD persists, member links update, rights matrix loads and applies grants/revocations with clear feedback. | Manual pass | `ui/src/pages/UsersPage.vue`; `ui/src/pages/TeamRosterPage.vue`; `ui/src/components/users/TeamRightsMatrixPanel.vue`; R3AKT team/member/routes; 2026-06-23 team roster and rights API retest below | Live API create/update/list/filter/get/link/unlink/grant/revoke/cleanup behavior passed with disposable team/member/rights artifacts. Browser proof for rendered team roster editors and the rights matrix panel remains open. |
| RCH-US-016 | Mission registry workspace | As a mission coordinator, I can create, inspect, update, archive/delete, parent, and browse missions. | Mission CRUD uses Python-compatible payloads, prevents invalid parent cycles, tracks changes, and keeps list/detail UI consistent. | Covered | `docs/release-contract-matrix.json`; `ui/src/pages/missions/MissionsLegacyPage.vue`; mission registry tests | Manual browser test for current legacy and domain workspaces. |
| RCH-US-017 | Mission domain object pages | As a power user, I can inspect mission-scoped domain objects from canonical object pages. | `/missions/:mission_uid/*` pages render overview, mission, topic, checklist, tasks, templates, teams, members, skills, assets, assignments, zones, events, changes, logs, snapshots, and audit events. | Needs manual test | `ui/src/router/index.ts`; `ui/src/pages/missions/domains/*`; 2026-06-22 UI route smoke below | Deep-link smoke passed for representative pages; create/edit/delete actions and every domain subpage still need manual verification. |
| RCH-US-018 | Mission audit and event timeline | As a coordinator, I can review mission audit/domain events and export audit data. | Audit and event routes return newest-first histories; UI expands details and exports JSON without data loss. | Needs manual test | `ui/src/pages/missions/MissionOverviewScreen.vue`; `ui/src/composables/missions/useAuditExportActions.ts`; `/api/r3akt/events`; 2026-06-23 mission audit/event export API retest below; 2026-06-23 mission audit browser proof attempt below | Live API/export-shaped retest passed against the configured DB-backed server. Browser proof for rendered expanded details and the actual download action remains blocked by the in-app browser controller timing out during local navigation. |
| RCH-US-019 | Checklists and checklist tasks | As a mission operator, I can create checklist runs, link to missions, add tasks, set task status, edit cells, and remove checklists/tasks. | Checklist CRUD, template-derived runs, task row/cell/status mutation, CSV import, feed publication, and mission link behavior persist and fan out as required. | Manual pass | `ui/src/pages/ChecklistsPage.vue`; `crates/r3akt-rch-core/src/lib.rs`; checklist route tests; 2026-06-23 checklist/template live API retest below | Fixed checklist response shape so template-derived checklist creation returns both `mission_id` and `mission_uid`, matching the UI raw record contract. Live DB/config-backed API retest passed template-derived checklist creation with mission link, task add/cell/style/status mutations, join/upload/feed publication, task delete, and cleanup. Browser proof for rendered checklist controls remains open. |
| RCH-US-020 | Checklist templates and CSV imports | As a planner, I can create, edit, clone, archive/delete templates and turn CSV imports into runnable checklists. | Template CRUD and CSV conversion preserve columns/tasks, show useful validation errors, and produce runnable checklists. | Manual pass | `ui/src/composables/useChecklistTemplateCrud.ts`; `ui/src/pages/ChecklistsPage.vue`; checklist template routes; 2026-06-23 checklist/template live API retest below | Live DB/config-backed API retest passed valid template create/patch/clone/delete, required `DUE_RELATIVE_DTG` validation, and CSV import preserving three columns and two task rows. Browser CSV picker/preview/delete confirmation proof remains open. |
| RCH-US-021 | Web map markers and zones | As an operator, I can view/create/update/delete markers and zones and associate them with missions. | Map renders, symbol registry loads, marker/zone CRUD persists, mission assignment works, and failures do not leave stale overlays. | Needs manual test | `ui/src/pages/WebMapPage.vue`; `/api/markers`; `/api/zones`; mission marker/zone routes; 2026-06-23 marker/zone live API retest below | Live API retest passed marker/zone create/list/patch/delete, marker alias normalization, validation errors, mission marker/zone link/unlink, marker telemetry, and event recording with disposable artifacts. Browser proof for MapLibre canvas rendering, marker radial menu, zone drawing/edit/delete, icon fallback, and stale overlay cleanup remains open. |
| RCH-US-022 | Files and images | As an operator, I can upload, list, preview, download, patch metadata, and delete files/images. | File/image metadata and raw APIs preserve content, pagination/order, preview/download, and delete/update UI state. | Covered | `docs/release-contract-matrix.json`; `ui/src/pages/FilesPage.vue`; file/image server tests; 2026-06-23 file/image live API retest below | Live API upload/list/raw/patch/delete flow passed with safe temporary file and PNG artifacts. Browser preview/download/delete confirmation remains a separate UI proof item while the in-app browser is unstable. |
| RCH-US-023 | Telemetry and latest snapshots | As an operator, I can ingest and review telemetry snapshots, flush telemetry, and see live telemetry stream updates. | Telemetry route persists Python-shaped entries, latest snapshots dedupe per identity, and stream auth/messages work. | Covered | `/Telemetry`; `/telemetry/stream`; telemetry tests in `crates/r3akt-rch-server/src/lib.rs` | Manual UI route for telemetry is indirect; map/dashboard verification needed. |
| RCH-US-024 | Emergency Action Messages | As a team lead, I can create, update, list, fetch latest, summarize team state, and delete EAM records. | EAM routes preserve callsign/team summary semantics; REM fanout sends supported EAM command shapes and skips inappropriate peers. | Manual pass | `docs/release-contract-matrix.json`; EAM tests in `crates/r3akt-rch-core/src/lib.rs` and server; 2026-06-23 Emergency Action Message API retest below | Live API create/list/filter/latest/get/summary/update/validation/delete flow passed with a disposable EAM and team. Browser proof for the embedded mission/member EAM controls remains open. |
| RCH-US-025 | Assets, assignments, skills, and requirements | As a logistics/resource manager, I can manage assets, assign them, manage skills, and define task skill requirements. | R3AKT asset/assignment/skill routes persist links, prevent invalid relationships, and UI domain pages display/edit them. | Manual pass | `docs/release-contract-matrix.json`; `ui/src/pages/missions/domains/*ObjectPage.vue`; core tests; 2026-06-23 assets/assignments/skills live API retest below | Live API create/update/list/filter/get/link/unlink/validation behavior passed with disposable logistics artifacts. Browser domain-object proof remains open, and public delete routes exist for assets but not for skills, assignments, or task skill requirements. |
| RCH-US-026 | TAK connector service | As an integrator, I can run TAK as a standalone sidecar and bridge RCH telemetry/chat to TAK and TAK CoT back to RCH markers. | TAK service owns socket lifecycle, supports TCP/UDP/TLS/protobuf profiles, retries sends, reports status, and uses RCH northbound HTTP. | Covered | `docs/release-contract-matrix.json` contracts `python-tak-compatible-behavior` and `rust-additive-standalone-tak-service`; `crates/r3akt-tak-connector` tests | External TAK target profile should be rerun for RC. |
| RCH-US-027 | Python install/import migration | As an existing Python RCH operator, I can install Rust RCH over a Python store and migrate config, identity, files, images, telemetry, and SQLite state. | First-start import discovers Python stores, copies runtime files safely, migrates rows additively, and does not mutate `rch-python`. | Covered | `docs/release-contract-matrix.json` contract `rust-additive-python-install-import`; `crates/r3akt-rch-server/src/main.rs`; migration tests | Manual install-over-Python rehearsal still useful. |
| RCH-US-028 | Packaging and desktop sidecars | As a release operator, I can build server packages and Tauri desktop artifacts with traceable metadata. | Server archives include server, TAK service, UI, config templates, manifests, checksums, and platform-specific desktop wrappers where supported. | Covered | `docs/release-readiness-audit.md`; `.github/workflows/rust-release.yml`; `scripts/build-rust-release-package.ps1`; `apps/rch-desktop` | Expanded macOS/Raspberry Pi matrix needs next release run. |
| RCH-US-029 | Release gates and CI | As a maintainer, I can rely on branch-protectable CI and local scripts to block regressions. | Format, clippy, workspace tests, release builds, audit, release readiness, local Reticulum live gates, and parity probes are documented and runnable. | Covered | `.github/workflows/rust-pr-quality.yml`; `.github/workflows/rust.yml`; `scripts/release-readiness.ps1`; PR #200 checks | Keep current branch checks green before PRs are marked ready. |
| RCH-US-030 | UI shell/navigation/layout | As an operator, I can navigate all main screens without console errors, missing assets, broken route guards, or unusable layouts. | Sidebar/header/routes render; public/protected routing is clear; browser refresh on deep links works through SPA fallback; responsive layout remains usable. | Manual pass | `ui/src/router/index.ts`; `ui/src/components/SidebarNav.vue`; `crates/r3akt-rch-server/src/lib.rs` UI fallback tests; 2026-06-22 UI route smoke below | Primary route and representative deep-link smoke passed in the in-app browser with zero console errors; keep responsive/mobile and action-specific UX tests open under the feature rows above. |

## Current Test Runs

| Run | Stories touched | Scope | Result | Errors / next action |
| --- | --- | --- | --- | --- |
| 2026-06-22 local UI route smoke | RCH-US-003, RCH-US-005, RCH-US-007, RCH-US-008, RCH-US-009, RCH-US-014, RCH-US-015, RCH-US-016, RCH-US-017, RCH-US-018, RCH-US-019, RCH-US-021, RCH-US-022, RCH-US-025, RCH-US-030 | In-app browser against `http://127.0.0.1:18080/` using the live local `RTH_Store` DB/config. Hard-refresh loaded `/`, `/missions`, `/missions/legacy`, `/missions/domain`, `/missions/assets`, `/missions/logs`, `/checklists`, `/webmap`, `/topics`, `/files`, `/chat`, `/users`, `/users/teams/members`, `/configure`, `/about`, `/connect`, and representative `/missions/57942bdb0269482ea76f3b2530dea6f9/*` deep links. | Pass for shell load, SPA fallback, route guards on loopback, main navigation visibility, and console-error smoke. Dashboard reached `ONLINE`/`LIVE` with PID `7748`; chat, files, topics, checklists, users, team roster, configure, about, mission legacy, and representative mission-domain pages rendered without browser console errors. | No confirmed product error in this slice. A short 1.5s hard-refresh wait produced false boot-screen failures; future route smoke should wait until `INITIALIZING RETICULUM BACKEND` disappears or a real timeout is reached. Action-level CRUD, destructive flows, WebMap canvas behavior, remote-auth redirect behavior, and mobile/responsive layout remain untested. |
| 2026-06-22 remote auth and connection retest | RCH-US-003 | In-app browser Connect flow against the live server using `http://127.0.0.1.nip.io:18080` and `ws://127.0.0.1.nip.io:18080` as remote-looking URLs that resolve to the local RCH instance. Used API key `manual-test`; restored settings to `http://127.0.0.1:18080` afterward. | Pass. Connect switched to Remote Connection mode, displayed `Remote backend requires authentication.`, and protected `/chat` redirected to `/connect?redirect=/chat` while unauthenticated. API Key mode displayed `API key is required for remote backend authentication.` until a key was entered. Login with `manual-test` redirected back to `/chat`; local restore showed Local Connection Settings and `Connection settings saved`. | No product error found. This validates remote gating and login mechanics without a true external hub; real remote TLS/DNS deployment should still be covered during staging or deployment tests. |
| 2026-06-22 public metadata and diagnostics smoke | RCH-US-004, RCH-US-005, RCH-US-006 | Read-only HTTP checks against `http://127.0.0.1:18080/` using `X-API-Key: manual-test` where protected endpoints require it. Checked `/Help`, `/Examples`, `/openapi.json`, `/openapi.yaml`, `/api/v1/app/info`, `/Status`, and `/diagnostics/runtime`. | All endpoints returned `200`. Python's case-sensitive JSON parser loaded `/openapi.json`; `/api/v1/app/info` exposed app/runtime metadata; `/Status` included chat/client/file/image/runtime counters; `/diagnostics/runtime` included Reticulum/ZMQ/persistence/outbound diagnostic keys. | Confirmed logistical error: PowerShell `ConvertFrom-Json` cannot parse `/openapi.json` because the path map contains case-only aliases (`/diagnostics/runtime` and `/Diagnostics/Runtime`). |
| 2026-06-22 dashboard runtime-control retest | RCH-US-004 | Rebuilt and restarted `target/release/r3akt-rch-server.exe` on `http://127.0.0.1:18080/` using `RTH_Store\rch_state.sqlite3`, `RTH_Store\config.ini`, API key `manual-test`, Reticulumd RPC `127.0.0.1:14243`, and LXMF ZMQ endpoints. Exercised `/Control/Status`, `/Control/Announce`, `/Control/Sync`, and the Dashboard `Sync` button in the in-app browser. | Pass after fixes. `/Control/Status` and the dashboard showed `PID: 5012` and `Port: 18080`; announce returned `announce sent`; failed propagation sync rendered `Propagation sync failed: outbound request failed: ... (os error 10060)` instead of a stale port placeholder or generic failure. | Do not mark propagation sync as a network success in this environment; the failure is legitimate because the configured propagation RPC path timed out. The UI now reports that detail clearly. |
| 2026-06-22 OpenAPI Windows parser retest | RCH-US-005, RCH-US-006 | Rebuilt and restarted `target/release/r3akt-rch-server.exe` with the live `RTH_Store` DB/config, API key `manual-test`, and the existing Reticulumd ZeroMQ endpoints. Retested `/openapi.json`, `/diagnostics/runtime`, and `/Diagnostics/Runtime` on `http://127.0.0.1:18080/`. | Pass. PowerShell `ConvertFrom-Json` now parses `/openapi.json`; exact `/diagnostics/runtime` is no longer a separate OpenAPI path; `/Diagnostics/Runtime` has `x-rch-aliases: ["/diagnostics/runtime"]`; case-insensitive duplicate path count is `0`; both diagnostics HTTP aliases return `200`. | Fixed logistical error. Keep both HTTP routes live for compatibility while using a single parser-safe OpenAPI path entry. |
| 2026-06-22 broadcast timeout fallback retest | RCH-US-009, RCH-US-010, RCH-US-013 | Live local `target/release/r3akt-rch-server.exe` on `http://127.0.0.1:18080/` using `RTH_Store\rch_state.sqlite3`, `RTH_Store\config.ini`, Reticulumd RPC `127.0.0.1:14243`, and LXMF ZMQ endpoints. Inspected user-reported message `2a2892b3227b427487308d53712dd163`; sent broadcast canaries `d3a43d4ff4844ccb9cb692d56a7b157c`, `89101f6a0bb04916b68aed1b31b1e21c`, and `991ee56574b04ca59c0a5bd173a4c5b0`. | Mixed. The user-reported `2a289...` row recovered to `propagated` with `reticulumd_dispatch_count=13` and six `sent: propagated resource` child rows. Canary `d3a43...` reproduced a direct `send_timeout` terminal failure before the fix. After the fixes, `89101...` moved from `direct`/`broadcast_direct` to queued `propagated`/`broadcast_direct_timeout_fallback`; `991ee...` stayed queued through attempt 10 with rate-limit delayed retry instead of failing at attempt 5. | Propagated dispatch is currently hitting `SDK_SECURITY_RATE_LIMITED: per-ip request rate limit exceeded`, so the latest canary remains queued with retry scheduled. Retest after the rate-limit window clears and confirm accepted propagated dispatch on phones/decks. |
| 2026-06-22 propagated broadcast callback retest | RCH-US-009, RCH-US-010 | User reported `2a2892b3227b427487308d53712dd163` as `Delivery Method=propagated`, `Delivery Policy Reason=broadcast_direct_timeout_fallback`, `Failure Reason=send_error`, `Route Type=broadcast`. Checked live `RTH_Store\rch_state.sqlite3` and added an HTTP-level `/internal/delivery-failure` regression for propagated broadcast fallback `send_error`. | Fixed in code, live retest still needed. The live row had already recovered in SQLite to `delivery_state=propagated`, `dispatch_status=accepted`, `attempts=5`, but the callback handler could still mark retry-eligible propagated broadcasts terminal failed. The new test verifies the failure callback returns `retry_scheduled`, stores `delivery_state=queued`, keeps `delivery_method=propagated`, and increments attempts to 5. | Restart the manual server with the new build, repeat broadcast canary against phones/decks, and confirm no transient terminal `failed/send_error` state appears for propagated fallback callbacks. |
| 2026-06-22 rate-limit budget retest | RCH-US-009, RCH-US-010, RCH-US-013 | Rebuilt and restarted `target/release/r3akt-rch-server.exe` on `http://127.0.0.1:18080/` using the live `RTH_Store` DB/config. USB phones `Pixel_7` and `SM_G950W` were attached; `network.reticulum.emergency` 1.1.2 was installed and then launched on both. `/Client` still listed `silkedeck`, `raphydeck`, and `corvodeck`, but their last-seen values were stale except `corvodeck` earlier on 2026-06-22. Sent canaries `3fc2f131af65410c82badf99d5ec44a8` and `dc8c20bc041b47f78c313f77eea916c1`. | Fixed queue behavior; phone-side receipt still blocked by SDK rate limit. Canary `3fc2...` reproduced the remaining bug: after rate-limit retries, a later `send_timeout` marked propagated fallback terminal `failed` at attempt 7. After the fix, canary `dc8c...` stayed `queued` as `propagated`/`broadcast_direct_timeout_fallback` through attempt 8 with `retry_scheduled=true` and `rate_limit_retry_budget=true`; global failed count stayed flat at 37. | Continue after the SDK rate-limit window clears and confirm the queued propagated canary reaches accepted/sent propagation and appears on the phones/decks. |
| 2026-06-23 propagated fallback retry-ceiling retest | RCH-US-009, RCH-US-010, RCH-US-013 | User saw another propagated broadcast failure card after a direct-timeout fallback. `/Events` showed the latest terminal failure was canary `dc8c20bc041b47f78c313f77eea916c1`, which had `rate_limit_retry_budget=true`, `attempts=30`, `delivery_method=propagated`, `delivery_policy_reason=broadcast_direct_timeout_fallback`, and `Failure Reason=send_error`. A fresh rebuilt canary `d0af7fcf-c651-4ecc-92f8-883845a3cff0` then showed the default-budget direct broadcast timeout was still retried as `direct` once before propagation. | Pass after fixes for server-side queue behavior. Direct broadcast/fanout dispatch timeouts now queue propagation before ordinary direct retry budget is considered. Propagated broadcast/fanout direct-timeout fallbacks now use an extended 120-attempt retry ceiling. After rebuilding/restarting PID `10352`, canary `996a8718-7aa4-4a7a-bee4-e72c05cbcc66` emitted `message_propagation_queued` on first timeout and then `message_delivery_retrying` as `propagated` / `broadcast_direct_timeout_fallback` at attempt 2; failed count stayed flat at 38. A later live canary `c869fd1f3ffc405081c019a609324e80` reproduced the user-visible direct timeout, switched to propagated fallback, and after the propagated timeout stayed `queued` with `retry_scheduled=true` instead of surfacing terminal `failed/send_error`. | Phone/deck receipt remains unproven in this rate-limited environment; continue watching for accepted/sent propagation once the SDK/RNS path stops timing out. |
| 2026-06-23 persisted broadcast fallback repair retest | RCH-US-009, RCH-US-010, RCH-US-013 | User reported message `2a2892b3227b427487308d53712dd163` as a propagated fallback `failed/send_error`. Decoded live SQLite message and system-event history, rebuilt/restarted the release binary on PID `16648`, and watched persisted broadcast fallback rows in `RTH_Store\rch_state.sqlite3`. | Pass for persisted queue repair. `2a289...` is currently `propagated` with `dispatch_status=accepted`, `reticulumd_dispatch_count=13`, and 13 propagated receipt targets; the pasted failed event was an older superseded system event. Eight stranded retryable broadcast fallback rows moved from terminal `failed` back to `queued`. After one timeout window, seven were queued with rate-limit backoff and one remained actively dispatching; none returned to terminal failed. | Continue live phone/deck stress after `SDK_SECURITY_RATE_LIMITED` clears and confirm repaired queued fallbacks reach accepted/sent propagation. |
| 2026-06-23 superseded broadcast failure event retest | RCH-US-009, RCH-US-010 | User again reported message `2a2892b3227b427487308d53712dd163` as `failed` / `propagated` / `broadcast_direct_timeout_fallback` with `send_error`. Rebuilt and restarted the live configured server on PID `5748`, checked `/Chat/Messages?limit=200`, `/Events`, and added an event replay regression. | Pass for canonical state and replay semantics. The reported message is currently `State=propagated`, `dispatch_status=accepted`, `reticulumd_dispatch_count=13`, and has 13 propagated receipt targets with six `sent: propagated resource`. `/Events` retained no current failure row for the ID and no `message_delivery_failed` rows. Recovered stale failure events now replay as `message_delivery_superseded` with `delivery_failure_superseded=true`, `current_state`, and `current_dispatch_status`. | The next pasted failure card proved a dashboard client-side stale-card merge gap, now covered by the 2026-06-23 dashboard stale broadcast failure card retest. Continue live phone/deck stress once rate limiting clears. |
| 2026-06-23 dashboard stale broadcast failure card retest | RCH-US-009, RCH-US-010, RCH-US-030 | User pasted the same `2a2892b3227b427487308d53712dd163` broadcast failure card after the superseded-event backend fix. Rechecked the live DB/config-backed server on PID `5748`, `/Chat/Messages?limit=500`, `/Events?limit=500`, and rebuilt the UI bundle served from `ui/dist`. A later recheck on PID `18144` again found the SQLite row `propagated`/`accepted` while the selected in-app browser tab was `about:blank` and browser navigation to the local UI timed out. | Fixed in UI and verified by focused test/build. The canonical row remains `State=propagated`, `dispatch_status=accepted`, `reticulumd_dispatch_count=13`, with 13 receipt targets; `/Events?limit=500` has zero events for that ID and zero current `message_delivery_failed` rows. Added `ui/src/stores/dashboard.spec.ts` and dashboard merge logic so a `message_delivery_superseded` event removes older `message_delivery_failed` rows for the same `MessageID`; `npm --prefix ui run test -- dashboard` and `npm --prefix ui run build` passed. Fresh MessagePack decoding showed newer terminal failed canaries were invalid-destination test rows, not retryable `send_error` broadcast fallbacks. | In-app browser control timed out while navigating to `http://127.0.0.1:18080/`, so rendered visual proof of the refreshed dashboard remains manual. The local server is running and HTTP `/` returns `200 text/html` with the rebuilt app bundle. |
| 2026-06-23 dashboard event snapshot reconciliation retest | RCH-US-009, RCH-US-010, RCH-US-030 | User again pasted the `2a2892b3227b427487308d53712dd163` broadcast card as `failed` / `propagated` / `broadcast_direct_timeout_fallback` with `send_error`. Live `RTH_Store\rch_state.sqlite3`, `/Chat/Messages?limit=500`, and `/Events?limit=500` were checked against PID `18144`. | Fixed an additional UI stale-state gap. The live message has exactly one current SQLite row and API row, both `State=propagated`, `dispatch_status=accepted`, `reticulumd_dispatch_count=13`, with six propagated child targets already `sent`; `/Events?limit=500` has no event for `2a289...` and no current `message_delivery_failed` event for it. Added dashboard store replacement from authoritative `/Events` snapshots so periodic refresh clears stale in-memory failure rows even if the tab missed the recovery event. Rebuilt `ui/dist`; the live server now serves `assets/index-D9w_tpHe.js`. | In-app browser navigation to the local UI timed out during verification, so rendered proof remains open. Refresh the browser tab before further manual event-feed checks. |
| 2026-06-23 post-budget fallback retry retest | RCH-US-009, RCH-US-010, RCH-US-013 | User reported another broadcast card for `2a2892b3227b427487308d53712dd163`, now showing `Delivery Method=propagated`, `Delivery Policy Reason=broadcast_direct_timeout_fallback`, and `Failure Reason=send_error`. Rechecked live SQLite/API state and inspected retryable propagated fallback rows that had exhausted prior retry ceilings. | Fixed backend retry semantics. The reported `2a289...` row is still canonical `propagated`/`accepted` with `reticulumd_dispatch_count=13` and no current `error`/`retry_reason`; the pasted failure was not the current DB/API state. Added tests so retryable propagated fallback `send_error`/timeout errors continue as queued propagation after the previous 120-attempt ceiling, already-failed retryable fallback rows are repair-due, active retry attempts clear stale `error`/`retry_reason`/`last_attempt_failed_at_ts_ms`, later success clears stale failed-at metadata, and `/Chat/Messages` hides stale failure keys for already-persisted successful rows. Restarting the fixed server repaired failed fallback rows `dc8c20bc041b47f78c313f77eea916c1` and `991ee56574b04ca59c0a5bd173a4c5b0` from terminal `failed` back to `queued`/`in_progress`; final PID `3680` returned `2a289...` as `propagated`/`accepted` with no serialized failure keys. | Live phones/decks are still blocked by the SDK/RNS rate-limit window; continue stress until queued propagated fallbacks reach accepted/sent propagation on the connected devices. |
| 2026-06-23 mission audit/event export API retest | RCH-US-018 | Live local `target/release/r3akt-rch-server.exe` on `http://127.0.0.1:18080/` using `RTH_Store\rch_state.sqlite3`, `RTH_Store\config.ini`, API key `manual-test`, and server PID `3680`. Created disposable mission `codex-us018-20260623014108`, explicit mission change `codex-us018-change-20260623014108`, and log entry `codex-us018-log-20260623014108`. | Pass for live API/export data behavior. `/api/r3akt/events?limit=50&include_payload=false` returned three newest-first mission events with no `payload` fields; `include_payload=true` returned the same mission events with payloads. `/api/r3akt/mission-changes?mission_uid=...&include_delta=false` stripped `delta`; `include_delta=true` returned the explicit change plus the expected log-derived `ADD_CONTENT` change. `/api/r3akt/log-entries?mission_uid=...` returned only the disposable log row. An export-shaped JSON object containing audit events, mission changes, and log entries round-tripped with counts intact. | Browser proof remains open for expanding detail rows in `MissionOverviewScreen.vue` and triggering the actual `mission-audit-*.json` download from `useAuditExportActions.ts`. |
| 2026-06-23 mission audit browser proof attempt | RCH-US-018 | In-app browser controller was reconnected to the selected `RCH UI` tab and pointed at the DB-backed local server. Tried direct navigation to `/missions?mission_uid=codex-us018-20260623014108`, then retried a lighter root `/` load while `/Status` still returned healthy runtime PID `3680`. | Blocked by test harness. Both navigation attempts timed out and reset the browser controller before a DOM snapshot could be captured, so rendered mission audit expansion and download proof could not be collected in this pass. | Keep RCH-US-018 as `Needs manual test` until a stable browser session proves detail expansion and `mission-audit-*.json` download. No additional product API bug was found. |
| 2026-06-23 checklist/template live API retest | RCH-US-019, RCH-US-020 | Live local `target/release/r3akt-rch-server.exe` on `http://127.0.0.1:18080/` using `RTH_Store\rch_state.sqlite3`, `RTH_Store\config.ini`, API key `manual-test`, Reticulumd RPC `127.0.0.1:14243`, and LXMF ZMQ endpoints. Created disposable run `codex-us019-20260623002534`; exercised template create/patch/clone/delete, template-derived checklist creation with mission link, task add, cell patch, row style, task status, join, upload, feed publish, CSV import, task delete, and cleanup. | Pass after one response-shape fix. Initial valid run reproduced a UI contract bug: template-derived checklist creation returned `mission_id` but omitted `mission_uid`, so UI raw records saw an empty mission link. Added regression `checklist_from_template_returns_mission_uid_alias`, fixed `checklist_value`, rebuilt/restarted PID `18144`, and reran the live slice. The retest passed with checklist `e7d98e65715e4af69dcdd3a6ae9b0f1b`, task `a6dc2dbc961d4076be2cccc8731bbba5`, CSV import `923d37d274744287ae75202ea65d7fdb`, and cleanup verification showed zero active disposable checklists/templates/missions. | Browser proof for rendered checklist modals, CSV picker/preview, and delete confirmations remains open; API behavior for this slice passed against the live configured server. |
| 2026-06-23 team roster and rights API retest | RCH-US-015 | Live local `target/release\r3akt-rch-server.exe` on `http://127.0.0.1:18080/` using `RTH_Store\rch_state.sqlite3`, `RTH_Store\config.ini`, and API key `manual-test`. Created disposable mission/team/member run `codex-us015-20260623025150`; exercised rights definitions, team create/update/list/filter/get, team mission link/list/unlink, member create/update/list/filter/get, member client link/list/unlink, rights subjects, operation grant/list/revoke, mission access assign/list/revoke, validation status probes, and cleanup. | Pass for live API behavior. Rights definitions included `mission.registry.log.read` and role bundles; team and member create/update persisted; list/filter/detail routes returned the disposable records; rights subjects listed the mission member; operation grant and mission access assignment appeared in filtered lists; revoke routes returned expected `granted=false` / `deleted=true`; validation probes returned expected 400/404 statuses; cleanup deleted member/team/mission and left zero active member/team/access rows for the prefix. | Browser proof for `UsersPage`, `TeamRosterPage`, and `TeamRightsMatrixPanel` create/edit/link/revoke feedback remains open. The pass confirmed role values must use canonical tokens such as `TEAM_LEAD`; display labels such as `Team Lead` are rejected with 400. |
| 2026-06-23 topics/subscribers partial CRUD retest | RCH-US-008 | In-app browser against `http://127.0.0.1:18080/topics` using the live `RTH_Store` DB/config. Reused the temporary `codex.rc.1782174326823` topic created during the interrupted prior pass, edited its name/description, added subscriber destination `1335df70880114d149c3ad8d63fb5dcd`, changed `Reject Tests` from `True` to `False`, then cleaned up through the same backend endpoints after the browser confirmation flow wedged. | Partial pass. Topic edit showed `Topic updated` and refreshed the card. Subscriber create showed `Subscriber added`, displayed `silkedeck`, the topic label, destination, and `Reject Tests=True`; subscriber patch showed `Subscriber updated` and `Reject Tests=False`. Cleanup via `DELETE /Subscriber?id=1335df70880114d149c3ad8d63fb5dcd` and `DELETE /Topic?id=e1990970bfb844148247b0b1e1738946` succeeded; follow-up API verification showed `topicPresent=0`, `subscriberPresent=0`, topic count `9`, subscriber count `7`. | Browser automation wedged around the UI delete confirmation after opening the delete flow, so UI delete confirmation is not proven. Retest topic/subscriber delete in a fresh browser session and cover topic asset attach/detach before marking RCH-US-008 Manual pass. |
| 2026-06-23 topic asset association API retest | RCH-US-008, RCH-US-022 | Live local `target/release/r3akt-rch-server.exe` on `http://127.0.0.1:18080/` using `RTH_Store\rch_state.sqlite3`, `RTH_Store\config.ini`, and API key `manual-test`. Created disposable topic `codex-us008-20260623004949`, explicit subscriber `codex-us008-20260623004949-subscriber`, random 32-hex destination, text file, and PNG image. | Pass for API behavior that backs the Topics page Manage Assets modal. `PATCH /File/{id}` and `PATCH /Image/{id}` persisted `TopicID=codex-us008-20260623004949`; `/File` and `/Image` lists showed the associations; patching the file with `TopicID=null` detached it; deleting the topic cleared the image `TopicID`; cleanup deleted file/image/subscriber/topic and follow-up list checks showed zero matching disposable topics/subscribers. | Rendered Manage Assets modal attach/detach feedback and topic/subscriber delete confirmation still need a clean browser pass. |
| 2026-06-23 client/identity API moderation retest | RCH-US-014 | Live local `target/release/r3akt-rch-server.exe` on `http://127.0.0.1:18080/` using `RTH_Store\rch_state.sqlite3`, `RTH_Store\config.ini`, and API key `manual-test`. Created disposable client identity `codex-rch-us014-20260622222002` through `POST /RTH`, exercised `POST /Client/{identity}/Ban`, `/Blackhole`, `/Unban`, checked `/Identities`, `/api/rem/peers`, `/Command/DumpRouting`, then removed it with `PUT /RTH`. | Partial pass. `/Client` increased from 13 to 14 after join and returned to 13 after leave. Ban returned `200` and `/Identities` showed `IsBanned=true`; Blackhole returned `200` and showed both `IsBanned=true` and `IsBlackholed=true`; Unban returned `200` and cleared both flags. `/api/rem/peers` returned an object with `items` and `effective_connected_mode`; `/Command/DumpRouting` returned a routing object with `destinations`. | The in-app browser path timed out twice before interaction (`Page.enable` timeout on the second attempt), so rendered Users/Identities/REM Peers/Routing tab switching and toast feedback remain unproven. No product code issue confirmed in this API slice. |
| 2026-06-23 file/image live API retest | RCH-US-022 | Live local `target/release/r3akt-rch-server.exe` on `http://127.0.0.1:18080/` using `RTH_Store\rch_state.sqlite3`, `RTH_Store\config.ini`, and API key `manual-test`. Uploaded `codex-us022.txt` and a tiny PNG through `/Chat/Attachment`, retrieved metadata and raw bytes through `/File/{id}`, `/File/{id}/raw`, `/Image/{id}`, and `/Image/{id}/raw`, patched `TopicID`, checked paginated `/File?page=1&per_page=2` and `/Image?page=1&per_page=2`, validated bad image content type and hash mismatch errors, then deleted all test artifacts. | Pass for live API behavior. Text file upload returned `FileID=52`, `MediaType=text/plain`, `Size=29`; PNG upload returned `FileID=53`, `MediaType=image/png`, `Size=68`. Raw downloads returned matching bytes and content types. Patch set `TopicID=codex-us022-topic` and a follow-up patch cleared it. Pagination returned Python-shaped `items/page/per_page/total/total_pages/has_next/has_previous`. Invalid image media type and wrong SHA-256 both returned `400` with useful `detail`. Delete returned `200`, raw endpoints returned `404`, and cleanup restored counts to files `4`, images `26` with no remaining `codex-us022` artifacts. | No product error found in the API slice. Browser preview modal, download trigger, upload picker, and delete confirmation still need rendered UI proof once the browser harness is healthy. |
| 2026-06-23 Emergency Action Message API retest | RCH-US-024 | Live local `target/release/r3akt-rch-server.exe` on `http://127.0.0.1:18080/` using `RTH_Store\rch_state.sqlite3`, `RTH_Store\config.ini`, and API key `manual-test`. Created disposable team `codex-eam-team-1782179686` and EAM callsign `Codex EAM 1782179686`; exercised create, list/filter, latest, get, team summary, update, validation, delete, and cleanup. | Pass for live API behavior. Create computed `overall_status=Yellow`; filtered list matched the EAM; latest/get returned the same record; team summary reported `total=1`, `active_total=1`, `yellow_total=1`; update changed `overall_status=Red`; mismatched path/body callsign returned `400`; confidence `1.1` returned `422 less_than_equal`; delete returned `200`, follow-up get returned `404`, and summary reported `active_total=0`, `deleted_total=1`. Cleanup removed active EAM and team (`activeEamMatches=0`, `teamMatches=0`); expected deleted EAM tombstone remains for summary/history. | Browser proof for the embedded mission/member EAM controls remains open. No product code issue confirmed in this API slice. |
| 2026-06-23 assets/assignments/skills live API retest | RCH-US-025 | Live local `target/release/r3akt-rch-server.exe` on `http://127.0.0.1:18080/` using `RTH_Store\rch_state.sqlite3`, `RTH_Store\config.ini`, and API key `manual-test`. Created disposable mission/team/member/checklist/task run `codex-us025-20260622232057`; exercised asset, skill, team-member skill, task skill requirement, assignment, assignment asset set/link/unlink, and invalid relationship validation. | Pass for live API behavior. Asset create/update/get/list-filter passed (`AVAILABLE` to `IN_USE`); skill create/update/list passed; team-member skill create/update/list-filter passed (`level=3` to `4`); task skill requirement create/update/list-filter passed (`minimum_level=2` to `5`, mandatory to optional); assignment create/update/list-filter passed (`PENDING` to `COMPLETE`); assignment asset set-empty, link, and unlink passed. Invalid asset member and invalid requirement skill both returned `404`. Cleanup deleted the asset, team member, team, and tombstoned the mission. | Browser domain-object pages still need rendered create/edit/link proof. Public delete routes are missing for skills, assignments, and task skill requirements, so namespaced skill/requirement/assignment rows remain as audit/history artifacts after the run. |
| 2026-06-23 marker/zone live API retest | RCH-US-021 | Live local `target/release/r3akt-rch-server.exe` on `http://127.0.0.1:18080/` using `RTH_Store\rch_state.sqlite3`, `RTH_Store\config.ini`, and API key `manual-test`. Created disposable mission `codex-us021-20260623040349-mission`, marker `623b9d1b7b8b4dfbbebce05aeadf7ca8`, alias marker `d0c1916c42f540409f014f3023276451`, and zone `7fcf60c32f8146b4a038d4a2be4be0d0`. | Pass for live API behavior. `/api/markers/symbols` exposed the default marker symbol; marker create/list/move/rename/delete passed; `type=car` and `symbol=uav` normalized to `vehicle`/`drone`; unsupported marker type returned `422`; zone create/list/patch/delete passed; one-point zone returned `422`; mission zone and marker link/list/unlink passed. `/Events` included marker create/update/delete/telemetry and mission marker/zone link/unlink events; `/Telemetry?since=0` retained the marker deleted telemetry row. Cleanup left zero active disposable markers, zones, or missions, with one expected deleted mission tombstone. | Browser proof remains open for MapLibre rendering, marker placement/radial menu, zone draw/edit/delete, mission assignment popovers, and stale overlay cleanup after delete. |
| 2026-06-22 config and Reticulum path retest | RCH-US-007, RCH-US-030 | Live local `target/release/r3akt-rch-server.exe` on `http://127.0.0.1:18080/` using `RTH_Store\rch_state.sqlite3` and `RTH_Store\config.ini`, launched without explicit `--reticulum-config-path` to test config-derived path resolution. Checked `/Config`, `/Config/Validate`, `/Reticulum/Config`, `/Reticulum/Config/Validate`, `/api/v1/app/info`, and the Configure page in the in-app browser. | Pass for read/validate and UI load. `/Config` returned the 1,162-byte hub config and valid INI validation; invalid hub text returned `valid=false` with parser errors. After the fix, `/api/v1/app/info.reticulum_config_path` resolved to `C:\Users\broth\.reticulum/config`; `/Reticulum/Config` returned the 640-byte real Reticulum config and valid validation. Configure loaded without console errors, showed Reticulum values such as `rmap` and `rmap.world`, and the active Reticulum header size no longer showed the stale 1,162-byte Hub payload. | Fixed logistical error: server startup now derives Reticulum config path from `[hub].reticulum_config_path` when the CLI flag is absent. Fixed UX error: Configure header payload size now follows the active module. Controlled apply/rollback test still required to avoid mutating the operator config unexpectedly. |
| 2026-06-22 controlled config apply/rollback retest | RCH-US-007 | Isolated temp server on `http://127.0.0.1:18081/` using temp hub and Reticulum config files under `%TEMP%`, API key `config-test`, and no Reticulum daemon. Tested `/Config` and `/Reticulum/Config` PUT plus no-body rollback, then used the Configure page against the same temp server for Hub apply/rollback. | Mixed, with one UX fix verified. API apply created backup files and persisted changes for both Hub and Reticulum configs; no-body rollback restored both files. Hub UI apply showed `Apply Result` and backup path; initial rollback restored the file but left the editor showing stale applied text. After the fix, Hub UI rollback refreshed the editor to the restored text and changed the status pill to `Restored`, with no console errors. | Fixed UX error: Hub and Reticulum config stores reload the restored file after rollback, and Configure status/validation follows the active module. Reticulum form UI apply/rollback still needs a clean browser interaction pass; direct switch interaction timed out in the browser harness. |
| 2026-06-23 Reticulum form and BOM config retest | RCH-US-007 | Isolated temp server on `http://127.0.0.1:18081/` using temp hub/Reticulum config files and API key `config-test`. Browser drove Connect through `127.0.0.1.nip.io:18081`, opened Configure > Reticulum, edited the temp `test_tcp.target_host`, applied, rolled back, and verified the file. Also launched a fresh temp server with a BOM-prefixed `config.ini` and no explicit `--reticulum-config-path`. | Pass after one parser fix. Reticulum form loaded the temp config, changed `target_host` from `example.invalid` to `example.org`, applied with a success toast, rolled back with a success toast, refreshed the editor back to `example.invalid`, and the temp file had `hasRestoredHost=true`, `hasAppliedHost=false`, `backupCount=1`. The BOM-prefixed hub config launch derived the expected Reticulum config path and returned the temp config. | Fixed logistical parser error: UTF-8 BOM at the start of Windows-generated `config.ini` no longer prevents `[hub].reticulum_config_path` discovery. |

## Expansion Notes

- `docs/release-contract-matrix.json` currently enumerates 14 contract groups
  and 166 HTTP routes. The table above uses feature-group rows first; during
  the testing loop, split any row when a route, UI action, or live-device
  behavior has a separate expected behavior or failure mode.
- `ui/IMPLEMENTATION_PLAN.md` appears stale against the current
  `ChecklistsPage.vue` implementation. Treat it as historical context only
  until manual testing verifies actual missing behavior.
- Current live-device story status is derived from
  `docs/release-live-stress-report.md` and the latest PR #200 canary evidence;
  rerun the two Columba phones plus embedded Reticulum peers before claiming RC
  completion.
