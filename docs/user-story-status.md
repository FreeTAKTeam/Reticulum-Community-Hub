# RCH Rust Canonical User Story Status

This is the single canonical user-story ledger for release-candidate feature
audit work. It tracks operator-facing features, expected behavior, current
evidence, test status, and errors found during manual or automated story runs.

Last updated: 2026-06-22

## Status Values

- `Inventory`: Story identified from code/docs, expected behavior drafted, test pass not started.
- `Covered`: Automated tests or prior release evidence cover the expected behavior.
- `Manual pass`: A human/browser/live-device pass verified the behavior.
- `Needs manual test`: Automated coverage exists or source behavior is known, but the user workflow still needs an end-to-end manual pass.
- `Error found`: A logistical, UX, runtime, or behavioral error is recorded and needs a fix.
- `Retest needed`: A fix landed; the story must be exercised again.
- `Blocked`: A dependency, device, credential, or external service prevents meaningful testing.

## Canonical Table

| ID | Feature / surface | User story | Expected behavior | Current status | Evidence source | Errors / next action |
| --- | --- | --- | --- | --- | --- | --- |
| RCH-US-001 | Python-compatible northbound HTTP routes | As an existing RCH API/UI client, I can call the Python 2.9.x northbound route set against the Rust server. | Rust exposes the route inventory in OpenAPI, preserves method availability, returns Python-shaped success/error envelopes, and does not regress required routes. | Covered | `docs/release-contract-matrix.json` contract `python-northbound-http-routes`; `docs/release-parity-report.md`; `crates/r3akt-rch-server/src/lib.rs`; `crates/r3akt-rch-server/tests/release_contract_matrix.rs` | Expand into route-family story rows during route-by-route manual testing. |
| RCH-US-002 | Rust additive HTTP/control routes | As an operator, I can use Rust-only control, stream, OpenAPI, and app-info routes without confusing them for Python parity requirements. | Rust additive routes remain exposed, protected where needed, documented in OpenAPI where applicable, and do not break Python-compatible surfaces. | Covered | `docs/release-contract-matrix.json` contract `rust-additive-http-routes`; `crates/r3akt-rch-server/src/lib.rs` | Manual UX test stream/control routes from browser/API client. |
| RCH-US-003 | Authentication and connection selection | As a remote operator, I can configure a hub URL/API key and only enter protected UI routes after a valid auth session. | Public routes remain accessible; protected routes redirect to Connect when remote auth is missing; validation failure gives a clear error. | Inventory | `ui/src/router/index.ts`; `ui/src/pages/ConnectPage.vue`; `ui/src/stores/connection.ts`; `/api/v1/auth/validate` | Needs browser test against local and remote-target modes. |
| RCH-US-004 | Dashboard/runtime control | As an operator, I can see runtime health and request backend, announce, and propagation sync actions. | Dashboard renders status, queue metrics, Reticulum runtime state, and action feedback without stale or misleading state. | Needs manual test | `ui/src/pages/DashboardPage.vue`; `/Status`; `/diagnostics/runtime`; control routes | Exercise controls against local server; record UX/logistical failures. |
| RCH-US-005 | Help, examples, and app info | As a new operator, I can inspect help, examples, app metadata, and route inventory without setup friction. | `/Help`, `/Examples`, `/api/v1/app/info`, `/openapi.json`, and `/openapi.yaml` respond with useful metadata and no auth surprises. | Error found | `docs/release-readiness-audit.md`; `crates/r3akt-rch-server/src/lib.rs`; release readiness smoke; 2026-06-22 public metadata smoke below | Raw endpoints return `200`, but PowerShell `ConvertFrom-Json` fails on `/openapi.json` because case-only path aliases such as `/diagnostics/runtime` and `/Diagnostics/Runtime` collide in PowerShell's case-insensitive object model. Fix OpenAPI alias/tooling strategy or provide a Windows-compatible export before RC. |
| RCH-US-006 | Runtime diagnostics | As a release engineer, I can diagnose persistence, workers, Reticulum, outbound queues, counters, receipts, and schema state. | `/diagnostics/runtime` reports configured services, process/worker state, queue depth, dispatch/receipt counts, Reticulum/ZMQ configuration, and SQLite schema details. | Manual pass | `docs/release-contract-matrix.json` contract `rust-additive-runtime-diagnostics`; `docs/release-readiness-audit.md`; server tests; 2026-06-22 public metadata smoke below | Read-only local endpoint smoke passed; deeper diagnostics accuracy should still be checked when exercising queue/Reticulum failures. |
| RCH-US-007 | Config and Reticulum config management | As an operator, I can read, validate, apply, and roll back hub and Reticulum configuration. | Config routes return text payloads, validate INI-shaped content, apply changes with backups, and roll back safely. | Covered | `docs/release-contract-matrix.json`; `ui/src/pages/ConfigurePage.vue`; `crates/r3akt-rch-server/src/lib.rs` config tests | Needs browser test for validate/apply/rollback UX copy and disabled states. |
| RCH-US-008 | Topics and subscribers | As an operator, I can create, list, patch, delete, associate, and subscribe topics/subscribers. | Topic IDs normalize consistently; subscribers persist; pagination and Python-compatible envelopes work; UI updates after mutations. | Covered | `docs/release-contract-matrix.json`; `crates/r3akt-rch-core/src/lib.rs`; `ui/src/pages/TopicsPage.vue` | Manual UI CRUD story still needed. |
| RCH-US-009 | Chat direct, fanout, broadcast, and attachments | As an operator, I can send direct, topic, broadcast, and attachment messages and inspect delivery state. | Outbound messages are persisted immediately, worker dispatches asynchronously, direct timeouts can fall back to propagation when policy allows, and list/detail metadata stays understandable. | Retest needed | `crates/r3akt-rch-server/src/lib.rs`; PR #200 live canary `14d2c744ab6d436eb17ff2ccd987e2be`; `ui/src/pages/ChatPage.vue` | Retest normal chat, attachment upload, topic fanout, broadcast fallback after PR #200 merges or on this branch. |
| RCH-US-010 | Outbound delivery queue and retry policy | As an operator, I can trust queued sends, retry budgets, receipt timeouts, partial fanout handling, and failure reasons. | Queue metadata accurately distinguishes queued, in-progress, accepted, propagated, delivered, failed, dropped, retry, timeout, and receipt-pending states. | Retest needed | `crates/r3akt-rch-server/src/lib.rs`; `docs/release-live-stress-report.md`; PR #200 | Continue live stress tests with phones/decks; record any ambiguous failure reason. |
| RCH-US-011 | Reticulumd ZeroMQ runtime | As a release engineer, I can run RCH against LXMF-rs reticulumd through ZeroMQ command/response endpoints. | Server dispatches normal RCH payloads, batched fanout, event polling, delivery status, and identity announce calls through the ZeroMQ SDK boundary. | Covered | `docs/release-contract-matrix.json` contract `rust-additive-reticulumd-runtime`; `crates/r3akt-transport-rns/src/lib.rs`; CI checks on PR #200 | Keep live-device coverage current after transport or dependency changes. |
| RCH-US-012 | Reticulum/LXMF legacy-compatible command surface | As a peer/client, I can send legacy/plugin command names and payloads that Rust understands like Python RCH. | Legacy command aliases, admin commands, telemetry request, attachments, and command reply shapes remain accepted where required. | Covered | `docs/release-contract-matrix.json` contract `python-lxmf-legacy-command-surface`; `crates/r3akt-rch-core/src/lib.rs`; `crates/r3akt-profile-rch/src/lib.rs` | Add manual inbound command story when live peer scripting is available. |
| RCH-US-013 | REM compatibility and southbound command fanout | As a REM phone/deck user, I receive supported mission, checklist, EAM, log, marker, and telemetry commands in REM-compatible LXMF fields. | RCH emits reduced `FIELD_COMMANDS` payloads, deduplicates peers, respects REM capability/mode, and reports delivery/partial failures clearly. | Manual pass | `docs/rem-southbound-interface.md`; `docs/release-live-stress-report.md`; `docs/release-readiness-audit.md` | Continue two-phone plus raphydeck/silkedeck/corvodeck stress; raphydeck presence still needs current verification. |
| RCH-US-014 | Client, identity, REM peer, and routing workspace | As an operator, I can inspect and manage clients, identities, REM peers, teams, routing, bans, blackholes, and joins. | UI lists clients/identities/REM peers; ban/blackhole/unban/remove/join actions call the correct API and reflect errors clearly. | Needs manual test | `ui/src/pages/UsersPage.vue`; `/Client`; `/Identities`; `/api/rem/peers`; `/Command/DumpRouting` | Browser-test all destructive/non-destructive actions with a safe test identity. |
| RCH-US-015 | Team roster and rights matrix | As a mission admin, I can manage teams, members, client links, and operation rights. | Team/member CRUD persists, member links update, rights matrix loads and applies grants/revocations with clear feedback. | Needs manual test | `ui/src/pages/UsersPage.vue`; `ui/src/pages/TeamRosterPage.vue`; `ui/src/components/users/TeamRightsMatrixPanel.vue`; R3AKT team/member/routes | Manual flow required; include permission edge cases. |
| RCH-US-016 | Mission registry workspace | As a mission coordinator, I can create, inspect, update, archive/delete, parent, and browse missions. | Mission CRUD uses Python-compatible payloads, prevents invalid parent cycles, tracks changes, and keeps list/detail UI consistent. | Covered | `docs/release-contract-matrix.json`; `ui/src/pages/missions/MissionsLegacyPage.vue`; mission registry tests | Manual browser test for current legacy and domain workspaces. |
| RCH-US-017 | Mission domain object pages | As a power user, I can inspect mission-scoped domain objects from canonical object pages. | `/missions/:mission_uid/*` pages render overview, mission, topic, checklist, tasks, templates, teams, members, skills, assets, assignments, zones, events, changes, logs, snapshots, and audit events. | Needs manual test | `ui/src/router/index.ts`; `ui/src/pages/missions/domains/*`; 2026-06-22 UI route smoke below | Deep-link smoke passed for representative pages; create/edit/delete actions and every domain subpage still need manual verification. |
| RCH-US-018 | Mission audit and event timeline | As a coordinator, I can review mission audit/domain events and export audit data. | Audit and event routes return newest-first histories; UI expands details and exports JSON without data loss. | Covered | `ui/src/pages/missions/MissionOverviewScreen.vue`; `ui/src/composables/missions/useAuditExportActions.ts`; `/api/r3akt/events` | Manual export/expanded-row test needed. |
| RCH-US-019 | Checklists and checklist tasks | As a mission operator, I can create checklist runs, link to missions, add tasks, set task status, edit cells, and remove checklists/tasks. | Checklist CRUD, template-derived runs, task row/cell/status mutation, CSV import, feed publication, and mission link behavior persist and fan out as required. | Needs manual test | `ui/src/pages/ChecklistsPage.vue`; `crates/r3akt-rch-core/src/lib.rs`; checklist route tests | Existing `ui/IMPLEMENTATION_PLAN.md` is stale; verify actual implemented behavior and update errors here. |
| RCH-US-020 | Checklist templates and CSV imports | As a planner, I can create, edit, clone, archive/delete templates and turn CSV imports into runnable checklists. | Template CRUD and CSV conversion preserve columns/tasks, show useful validation errors, and produce runnable checklists. | Needs manual test | `ui/src/composables/useChecklistTemplateCrud.ts`; `ui/src/pages/ChecklistsPage.vue`; checklist template routes | Browser-test CSV edge cases and delete/archive UX. |
| RCH-US-021 | Web map markers and zones | As an operator, I can view/create/update/delete markers and zones and associate them with missions. | Map renders, symbol registry loads, marker/zone CRUD persists, mission assignment works, and failures do not leave stale overlays. | Needs manual test | `ui/src/pages/WebMapPage.vue`; `/api/markers`; `/api/zones`; mission marker/zone routes | Browser-test MapLibre rendering, canvas, marker icon fallback, and zone edit/delete. |
| RCH-US-022 | Files and images | As an operator, I can upload, list, preview, download, patch metadata, and delete files/images. | File/image metadata and raw APIs preserve content, pagination/order, preview/download, and delete/update UI state. | Covered | `docs/release-contract-matrix.json`; `ui/src/pages/FilesPage.vue`; file/image server tests | Manual browser test upload/preview/delete with safe files. |
| RCH-US-023 | Telemetry and latest snapshots | As an operator, I can ingest and review telemetry snapshots, flush telemetry, and see live telemetry stream updates. | Telemetry route persists Python-shaped entries, latest snapshots dedupe per identity, and stream auth/messages work. | Covered | `/Telemetry`; `/telemetry/stream`; telemetry tests in `crates/r3akt-rch-server/src/lib.rs` | Manual UI route for telemetry is indirect; map/dashboard verification needed. |
| RCH-US-024 | Emergency Action Messages | As a team lead, I can create, update, list, fetch latest, summarize team state, and delete EAM records. | EAM routes preserve callsign/team summary semantics; REM fanout sends supported EAM command shapes and skips inappropriate peers. | Covered | `docs/release-contract-matrix.json`; EAM tests in `crates/r3akt-rch-core/src/lib.rs` and server | Manual EAM UI path is embedded in mission/users flows; identify exact UI control in testing. |
| RCH-US-025 | Assets, assignments, skills, and requirements | As a logistics/resource manager, I can manage assets, assign them, manage skills, and define task skill requirements. | R3AKT asset/assignment/skill routes persist links, prevent invalid relationships, and UI domain pages display/edit them. | Covered | `docs/release-contract-matrix.json`; `ui/src/pages/missions/domains/*ObjectPage.vue`; core tests | Manual domain-object CRUD needed. |
| RCH-US-026 | TAK connector service | As an integrator, I can run TAK as a standalone sidecar and bridge RCH telemetry/chat to TAK and TAK CoT back to RCH markers. | TAK service owns socket lifecycle, supports TCP/UDP/TLS/protobuf profiles, retries sends, reports status, and uses RCH northbound HTTP. | Covered | `docs/release-contract-matrix.json` contracts `python-tak-compatible-behavior` and `rust-additive-standalone-tak-service`; `crates/r3akt-tak-connector` tests | External TAK target profile should be rerun for RC. |
| RCH-US-027 | Python install/import migration | As an existing Python RCH operator, I can install Rust RCH over a Python store and migrate config, identity, files, images, telemetry, and SQLite state. | First-start import discovers Python stores, copies runtime files safely, migrates rows additively, and does not mutate `rch-python`. | Covered | `docs/release-contract-matrix.json` contract `rust-additive-python-install-import`; `crates/r3akt-rch-server/src/main.rs`; migration tests | Manual install-over-Python rehearsal still useful. |
| RCH-US-028 | Packaging and desktop sidecars | As a release operator, I can build server packages and Tauri desktop artifacts with traceable metadata. | Server archives include server, TAK service, UI, config templates, manifests, checksums, and platform-specific desktop wrappers where supported. | Covered | `docs/release-readiness-audit.md`; `.github/workflows/rust-release.yml`; `scripts/build-rust-release-package.ps1`; `apps/rch-desktop` | Expanded macOS/Raspberry Pi matrix needs next release run. |
| RCH-US-029 | Release gates and CI | As a maintainer, I can rely on branch-protectable CI and local scripts to block regressions. | Format, clippy, workspace tests, release builds, audit, release readiness, local Reticulum live gates, and parity probes are documented and runnable. | Covered | `.github/workflows/rust-pr-quality.yml`; `.github/workflows/rust.yml`; `scripts/release-readiness.ps1`; PR #200 checks | Keep current branch checks green before PRs are marked ready. |
| RCH-US-030 | UI shell/navigation/layout | As an operator, I can navigate all main screens without console errors, missing assets, broken route guards, or unusable layouts. | Sidebar/header/routes render; public/protected routing is clear; browser refresh on deep links works through SPA fallback; responsive layout remains usable. | Manual pass | `ui/src/router/index.ts`; `ui/src/components/SidebarNav.vue`; `crates/r3akt-rch-server/src/lib.rs` UI fallback tests; 2026-06-22 UI route smoke below | Primary route and representative deep-link smoke passed in the in-app browser with zero console errors; keep responsive/mobile and action-specific UX tests open under the feature rows above. |

## Current Test Runs

| Run | Stories touched | Scope | Result | Errors / next action |
| --- | --- | --- | --- | --- |
| 2026-06-22 local UI route smoke | RCH-US-003, RCH-US-005, RCH-US-007, RCH-US-008, RCH-US-009, RCH-US-014, RCH-US-015, RCH-US-016, RCH-US-017, RCH-US-018, RCH-US-019, RCH-US-021, RCH-US-022, RCH-US-025, RCH-US-030 | In-app browser against `http://127.0.0.1:18080/` using the live local `RTH_Store` DB/config. Hard-refresh loaded `/`, `/missions`, `/missions/legacy`, `/missions/domain`, `/missions/assets`, `/missions/logs`, `/checklists`, `/webmap`, `/topics`, `/files`, `/chat`, `/users`, `/users/teams/members`, `/configure`, `/about`, `/connect`, and representative `/missions/57942bdb0269482ea76f3b2530dea6f9/*` deep links. | Pass for shell load, SPA fallback, route guards on loopback, main navigation visibility, and console-error smoke. Dashboard reached `ONLINE`/`LIVE` with PID `7748`; chat, files, topics, checklists, users, team roster, configure, about, mission legacy, and representative mission-domain pages rendered without browser console errors. | No confirmed product error in this slice. A short 1.5s hard-refresh wait produced false boot-screen failures; future route smoke should wait until `INITIALIZING RETICULUM BACKEND` disappears or a real timeout is reached. Action-level CRUD, destructive flows, WebMap canvas behavior, remote-auth redirect behavior, and mobile/responsive layout remain untested. |
| 2026-06-22 public metadata and diagnostics smoke | RCH-US-004, RCH-US-005, RCH-US-006 | Read-only HTTP checks against `http://127.0.0.1:18080/` using `X-API-Key: manual-test` where protected endpoints require it. Checked `/Help`, `/Examples`, `/openapi.json`, `/openapi.yaml`, `/api/v1/app/info`, `/Status`, and `/diagnostics/runtime`. | All endpoints returned `200`. Python's case-sensitive JSON parser loaded `/openapi.json`; `/api/v1/app/info` exposed app/runtime metadata; `/Status` included chat/client/file/image/runtime counters; `/diagnostics/runtime` included Reticulum/ZMQ/persistence/outbound diagnostic keys. | Confirmed logistical error: PowerShell `ConvertFrom-Json` cannot parse `/openapi.json` because the path map contains case-only aliases (`/diagnostics/runtime` and `/Diagnostics/Runtime`). |

## Expansion Notes

- `docs/release-contract-matrix.json` currently enumerates 14 contract groups
  and 166 HTTP routes. The table above uses feature-group rows first; during
  the testing loop, split any row when a route, UI action, or live-device
  behavior has a separate expected behavior or failure mode.
- `ui/IMPLEMENTATION_PLAN.md` appears stale against the current
  `ChecklistsPage.vue` implementation. Treat it as historical context only
  until manual testing verifies actual missing behavior.
- Current live-device story status is derived from
  `docs/release-live-stress-report.md` and the latest PR #200 canary evidence;
  rerun the two Columba phones plus embedded Reticulum peers before claiming RC
  completion.
